# Cache instance — everything here is reconstructible from MySQL.
# Losing the whole dataset is an availability event, never a correctness one,
# so we trade durability for speed and let it evict under pressure. (ADR-002)

save ""
appendonly no

maxmemory 256mb
maxmemory-policy allkeys-lru
lazyfree-lazy-eviction yes
